name: pr-validation

on:
  pull_request:
    branches:
      - main


jobs:
  validate:
  runs-on: ubuntu-latest

  steps:
    - name: Checkout code
      uses: actions/checkout

    - name: Print PR validataion
      run: echo "PR validation triggered"